---
- name: cleanup | remediate_cifs | Restore /etc/fstab
  ansible.builtin.copy:
    src: /etc/fstab.leapp_test_backup
    dest: /etc/fstab
    remote_src: true
    mode: preserve

- name: cleanup | remediate_cifs | Remove backup
  ansible.builtin.file:
    path: /etc/fstab.leapp_test_backup
    state: absent
...
